Step:
1. Open and run all code in SGJobData.ipynb to get the cleaned data in parquet format
2. In terminal, change to this folder and run following:
    ```bash
    conda activate pds
    streamlit run app.py
    ```
3. Open browser to explore the data via streamlit